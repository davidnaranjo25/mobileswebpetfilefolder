<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logofitpet"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logofitpet"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitPet">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".CatalogoActivity"
            android:exported="false" />

        <activity
            android:name=".RutinaDetalleActivity"
            android:exported="false" />

        <activity
            android:name=".RegistroActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".AdminMenuActivity"
            android:exported="false" />

        <activity
            android:name=".AgregarAdminActivity"
            android:exported="false" />

        <activity
            android:name=".AprobarVeterinariosActivity"
            android:exported="false" />

        <!-- Agrega aquí la nueva actividad para veterinarios -->
        <activity
            android:name=".VeterinariosActivity"
            android:exported="false" />

    </application>
</manifest>